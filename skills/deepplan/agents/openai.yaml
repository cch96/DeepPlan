interface:
  display_name: "DeepPlan"
  short_description: "Evidence-backed plan convergence"
  default_prompt: "Use $deepplan to ground evidence, compare alternatives, name optimization axes, and return no-edit when no behavior delta exists."
dependencies:
  tools:
    - type: "mcp"
      value: "openaiDeveloperDocs"
      description: "OpenAI Docs MCP server for Codex, skill, plugin, API, and SDK contract checks"
      transport: "streamable_http"
      url: "https://developers.openai.com/mcp"
