interface:
  display_name: "DeepPlan"
  short_description: "Evidence-backed plan convergence"
  default_prompt: "Use $deepplan to ground local evidence, compare alternatives, name the optimization axis for broad improve requests, check actionability, and converge one plan before execution."
dependencies:
  tools:
    - type: "mcp"
      value: "openaiDeveloperDocs"
      description: "OpenAI Docs MCP server for Codex, skill, plugin, API, and SDK contract checks"
      transport: "streamable_http"
      url: "https://developers.openai.com/mcp"
